{
    "inputs": [
        {
            "command-line-flag": "--numberOfHistogramLevels",
            "name": "numberOfHistogramLevels",
            "default-value": "128",
            "required": true,
            "type": "number",
            "description": "The number of hisogram levels to use"
        },
        {
            "command-line-flag": "--numberOfMatchPoints",
            "name": "numberOfMatchPoints",
            "default-value": "10",
            "required": true,
            "type": "number",
            "description": "The number of match points to use"
        },
        {
            "name": "thresholdAtMeanIntensity",
            "true-value": "--threshold",
            "required": true,
            "default-value": "false",
            "type": "boolean",
            "description": "If on, only pixels above the mean in each volume are thresholded."
        },
        {
            "required": true,
            "type": "string",
            "name": "inputVolume",
            "default-value": "",
            "description": "Input volume to be filtered"
        },
        {
            "required": true,
            "type": "string",
            "name": "referenceVolume",
            "default-value": "",
            "description": "Input volume whose histogram will be matched"
        },
        {
            "required": true,
            "type": "string",
            "name": "outputVolume",
            "default-value": "",
            "description": "Output volume. This is the input volume with intensities matched to the reference volume."
        }
    ],
    "description": "Normalizes the grayscale values of a source image based on the grayscale values of a reference image.  This filter uses a histogram matching technique where the histograms of the two images are matched only at a specified number of quantile values.\n\nThe filter was orginally designed to normalize MR images of the sameMR protocol and same body part. The algorithm works best if background pixels are excluded from both the source and reference histograms.  A simple background exclusion method is to exclude all pixels whose grayscale values are smaller than the mean grayscale value. ThresholdAtMeanIntensity switches on this simple background exclusion method.\n\nNumber of match points governs the number of quantile values to be matched.\n\nThe filter assumes that both the source and reference are of the same type and that the input and output image type have the same number of dimension and have scalar pixel types.",
    "image": "quay.io/fedorov/slicerdockers:4.8.1",
    "info-url": "http://wiki.slicer.org/slicerWiki/index.php/Documentation/Nightly/Modules/HistogramMatching",
    "name": "Histogram Matching",
    "command-line": "/opt/slicer/Slicer --launch HistogramMatching #numberOfHistogramLevels# #numberOfMatchPoints# #thresholdAtMeanIntensity# #inputVolume# #referenceVolume# #outputVolume#",
    "xnat": [],
    "label": "Histogram Matching",
    "version": "0.1.0.$Revision: 24799 $(alpha)",
    "schema-version": "1.0",
    "type": "docker"
}